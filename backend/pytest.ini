[pytest]
# Add the backend/ directory to sys.path so that "from app.xxx import ..." works
pythonpath = .

# Test discovery
testpaths = tests

# Automatically apply the asyncio event loop to all async tests
# (requires pytest-asyncio >= 0.21)
asyncio_mode = auto

# Short traceback for cleaner output
addopts = -v --tb=short
